import * as Lucide from 'lucide-react';
console.log(Object.keys(Lucide).filter(key => key.toLowerCase().includes('git')));
console.log(Object.keys(Lucide).filter(key => key.toLowerCase().includes('twitter')));
console.log(Object.keys(Lucide).filter(key => key.toLowerCase().includes('linkedin')));
